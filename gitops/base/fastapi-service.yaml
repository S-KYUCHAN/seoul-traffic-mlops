apiVersion: v1
kind: Service
metadata:
  name: seoul-traffic-fastapi
  namespace: kubeflow-user-example-com
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: seoul-traffic-fastapi
  type: ClusterIP
