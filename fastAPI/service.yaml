apiVersion: v1
kind: Service
metadata:
  name: seoul-traffic-fastapi
  namespace: kubeflow-user-example-com
spec:
  selector:
    app: seoul-traffic-fastapi
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP  # 클러스터 내부에서만 접근, 외부는 port-forward로 테스트